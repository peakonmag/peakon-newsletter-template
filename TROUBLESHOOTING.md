# 오류 해결

먼저 GitHub `Actions` → `1. 설정 연결 검사` → `Run workflow`를 실행하고,
완료된 실행의 Summary를 확인하세요.

## 필수 환경 변수가 비어 있습니다

GitHub 저장소의 `Settings` → `Secrets and variables` → `Actions`에서
다음 네 이름이 정확히 존재하는지 확인합니다.

- `NAVER_CLIENT_ID`
- `NAVER_CLIENT_SECRET`
- `NOTION_TOKEN`
- `NOTION_ROOT_PAGE_URL`

대소문자, 밑줄, 앞뒤 공백을 확인합니다. 값은 Variables가 아니라
Repository Secrets에 등록합니다.

## 네이버 API 인증에 실패했습니다

- 네이버 클라우드 플랫폼 콘솔의 `Application Services` →
  `NAVER API HUB` → `Application`에서 Client ID와 Client Secret을 다시
  확인합니다.
- 기존 NAVER Developers Center 인증 정보가 아닌 NAVER API HUB에서 발급한
  인증 정보인지 확인합니다.
- 해당 Application에 뉴스 검색 API가 선택되어 있는지 확인합니다.
- Secret을 재발급했다면 GitHub의 기존 Secret도 새 값으로 업데이트합니다.
- NAVER API HUB의 일일 호출 한도와 직접 설정한 이용 한도를 초과하지 않았는지
  확인합니다.

## Notion 인증에 실패했습니다

- Notion Integration의 내부 Token 전체를 다시 복사합니다.
- 템플릿을 복제한 워크스페이스와 Integration의 워크스페이스가 같은지
  확인합니다.
- Token을 재발급했다면 `NOTION_TOKEN` Secret도 교체합니다.

## 루트 페이지에 접근하지 못했습니다

- `NOTION_ROOT_PAGE_URL`이 두 데이터 소스를 포함하는 부모 대시보드 링크인지
  확인합니다.
- 데이터베이스 보기, 데이터베이스 행, 뉴스 기사 행의 링크를 넣지 않습니다.
- 부모 페이지의 `•••` → `연결 추가`에서 Integration을 연결합니다.
- 연결할 때 하위 페이지 접근도 허용했는지 확인합니다.

## 데이터 소스를 찾지 못했습니다

부모 페이지 아래에 정확히 다음 이름의 데이터 소스가 있어야 합니다.

- `키워드 설정`
- `뉴스 아카이브`

철자와 띄어쓰기를 원래대로 돌립니다. 링크드 뷰만 있고 실제 데이터 소스가
다른 접근 불가능한 페이지에 있지 않은지도 확인합니다.

## 같은 이름의 데이터 소스가 여러 개 있습니다

코드는 같은 이름 중 하나를 임의로 선택하지 않습니다. 복제하거나 테스트하면서
생긴 데이터 소스의 이름을 바꾸거나 불필요한 복사본을 휴지통으로 옮긴 뒤 다시
검사합니다.

## 속성이 없거나 유형이 잘못되었습니다

[notion-template-spec.md](notion-template-spec.md)의 표와 오류 메시지에 표시된
현재 유형을 비교합니다. 속성 이름만 같은 것으로는 충분하지 않고 유형도 정확해야
합니다. 복구가 어렵다면 새 템플릿을 다시 복제하는 것이 가장 빠릅니다.

## 활성 키워드가 0개입니다

Notion `키워드 설정`에서 다음을 확인합니다.

- `키워드`가 비어 있지 않다.
- `사용` 체크박스가 켜져 있다.
- `최근 시간 (Hour)`이 비어 있거나 0보다 큰 숫자다.
- `최대 기사 수`가 비어 있거나 1~100의 정수다.

활성 키워드가 0개여도 연결 검사는 성공할 수 있지만 뉴스는 수집되지 않습니다.

## 실행은 성공했지만 뉴스가 추가되지 않습니다

다음 상황은 정상일 수 있습니다.

- 최근 시간 안에 검색 결과가 없다.
- 검색된 기사가 모두 `뉴스 아카이브`에 이미 저장되어 있다.
- 키워드의 `최대 기사 수`가 너무 작다.
- 네이버 검색 결과에 발행 시각을 확인할 수 있는 기사가 없다.

먼저 `최근 시간 (Hour)`을 일시적으로 늘리고 새 키워드로 버튼 실행을
시험해 보세요.

## 뉴스가 중복 저장되는 것 같습니다

`뉴스 아카이브`의 `기사 키` 속성이 삭제되거나 유형이 바뀌지 않았는지
확인합니다. 사용자에게 보이지 않도록 숨겨도 되지만 데이터 소스에서는
`rich_text` 속성으로 반드시 존재해야 합니다.

## 예약 실행이 보이지 않습니다

- 워크플로가 기본 브랜치에 있는지 확인합니다.
- `Actions` → `2. 뉴스 수집`에서 `Enable workflow` 버튼이 보이면 누릅니다.
- GitHub Actions는 혼잡할 때 예약 시각보다 늦게 시작할 수 있습니다.
- 공개 저장소는 60일간 활동이 없으면 예약 워크플로가 자동으로 꺼질 수 있습니다.

## 도움을 요청할 때

실패한 Actions 실행 링크, 실패한 단계 이름, 비밀값을 지운 오류 문구를
전달하세요. Token, Client Secret, `.env` 내용은 절대 전달하지 않습니다.
