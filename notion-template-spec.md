# Notion 템플릿 명세

`NOTION_ROOT_PAGE_URL`로 지정한 하나의 부모 대시보드 아래에 다음 두 데이터
소스가 있어야 합니다. 데이터 소스 ID는 템플릿을 복제할 때 달라지므로 코드가
이름으로 탐색합니다.

## 키워드 설정

데이터 소스 이름은 정확히 `키워드 설정`이어야 합니다.

| 속성명 | Notion 속성 유형 | 필수 | 용도 |
| --- | --- | --- | --- |
| `키워드` | `title` | 예 | 검색할 문구. 앞뒤 공백은 제거됩니다. |
| `사용` | `checkbox` | 예 | 켜진 행만 검색합니다. |
| `최근 시간 (Hour)` | `number` | 예 | 실행 시점 기준 시간 범위. 빈 값은 24시간입니다. |
| `최대 기사 수` | `number` | 예 | 키워드별 요청 수. 빈 값은 10, 최댓값은 100입니다. |
| `마지막 실행` | `date` | 예 | 해당 키워드가 마지막으로 실행된 시각을 자동 기록합니다. |

영문 대소문자만 다른 동일 키워드는 먼저 발견된 한 행만 사용합니다.

## 뉴스 아카이브

데이터 소스 이름은 정확히 `뉴스 아카이브`여야 합니다.

| 속성명 | Notion 속성 유형 | 필수 | 용도 |
| --- | --- | --- | --- |
| `제목` | `title` | 예 | HTML을 정리한 기사 제목입니다. |
| `기사 키` | `rich_text` | 예 | 실행 간 중복 검사용 SHA-256 값입니다. 기본 보기에서는 숨깁니다. |
| `원문 URL` | `url` | 예 | 언론사 원문 링크입니다. |
| `네이버 링크` | `url` | 예 | 네이버 뉴스 링크입니다. |
| `발행 시각` | `date` | 예 | 네이버 검색 결과에 포함된 기사 발행 시각입니다. |
| `수집 시각` | `date` | 예 | 이 프로그램이 Notion에 저장한 시각입니다. |
| `검색 키워드` | `multi_select` | 예 | 기사를 발견한 모든 검색 키워드 태그입니다. |
| `검색 요약` | `rich_text` | 예 | 네이버 검색 API description을 정리한 값입니다. |
| `상태` | `select` | 예 | 기본 저장 값은 `신규`입니다. |

권장 `상태` 옵션은 `신규`, `검토 중`, `보관`입니다. 옵션 색상은 검사 대상이
아닙니다.

## 사용자에게 권장하는 보기

- `키워드 설정`: `키워드`, `사용`, `최근 시간 (Hour)`, `최대 기사 수`,
  `마지막 실행`을 표시합니다.
- `뉴스 아카이브`: `제목`, `원문 URL`, `검색 키워드`, `발행 시각`, `상태`를
  표시합니다.
- `기사 키`는 삭제하지 말고 보기에서만 숨깁니다.
- 각 뉴스 행의 하위 페이지에는 `검색 요약`, 검색 요약의 성격 안내,
  원문·네이버 링크가 들어갑니다.

`검색 요약`은 AI 요약 또는 기사 전문 요약이 아닙니다.
